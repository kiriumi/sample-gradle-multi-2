apply plugin: 'org.flywaydb.enterprise.flyway'

flyway {
    url = dbJdbcUrl
    user = dbUser
    password = dbPassword
    schemas = dbSchemas.split()
    locations = [ "filesystem:${projectDir}/src/main/db/migration" ] // デフォルトはsrc/main/db/migration
}
