plugins {
    id 'war'
    id 'eclipse-wtp'
    id 'distribution'
}

apply from: '../distribution.gradle'

dependencies {
    implementation project(':base-web')
    implementation project(':biz-domain')
}
